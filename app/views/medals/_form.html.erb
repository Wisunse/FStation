<%= form_with(model: medal, local: true) do |form| %>
  <% if medal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medal.errors.count, "error") %> prohibited this medal from being saved:</h2>

      <ul>
      <% medal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
